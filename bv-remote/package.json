{
  "name": "bv-remote",
  "private": true,
  "version": "0.1.0",
  "description": "BV Remote — the device-side agent for the projectBV fleet. The admin UI lives in the projectBV website (web/tools/remote). Build the installer with: cd agent && npm install && npm run dist"
}
